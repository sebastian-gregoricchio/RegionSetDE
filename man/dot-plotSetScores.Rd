% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSets.R
\name{.plotSetScores}
\alias{.plotSetScores}
\title{.plotSetScores}
\usage{
.plotSetScores(
  setScores,
  set = NULL,
  groupBy = NULL,
  groupOrder = NULL,
  comparisons = NULL,
  annotate = TRUE,
  colours = NULL,
  baseColours = NULL,
  title = NULL,
  subtitle = NULL,
  legendPosition = "none",
  baseSize = 12
)
}
\arguments{
\item{setScores}{\code{RegionSetDE.setScores} object.}

\item{set}{Character vector with the names of the region sets to draw. Default: \code{NULL}, all of them.}

\item{groupBy}{String with the name of a column of the sample metadata driving the colour. Default: \code{NULL}, the sample itself.}

\item{groupOrder}{Character vector with the levels of \code{groupBy} in the order they must appear. Default: \code{NULL}, alphabetical.}

\item{comparisons}{List of character vectors of length two, naming the pairs to annotate. Default: \code{NULL}, every pair that was compared.}

\item{annotate}{Logical value to indicate whether the brackets must be drawn. Default: \code{TRUE}.}

\item{colours}{Named character vector with one colour per sample. Default: \code{NULL}, shades built from the groups.}

\item{baseColours}{Character vector with one base colour per group. Default: \code{NULL}.}

\item{title}{String with the title of the plot, rendered as markdown. Default: \code{NULL}.}

\item{subtitle}{String with the subtitle of the plot, rendered as markdown. Default: \code{NULL}.}

\item{legendPosition}{String with the position of the legend. Default: \code{"none"}.}

\item{baseSize}{Numeric value with the base font size. Default: \code{12}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws a \code{RegionSetDE.setScores} object: one point per library per set, the points of a library joined across the sets, and a bracket carrying the paired difference and its adjusted p-value.
}
\details{
The lines joining the points are not decoration. The comparison behind the bracket is paired, one difference per library, and a figure drawing the sets as independent clouds would be showing a test other than the one whose p-value it carries. Three lines running parallel are what a small difference with a decisive paired p-value looks like; three lines crossing are why the interval on that difference is wide.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
