# .universeCovariates

Assembles the width and the width-adjusted abundance of every row, the
two covariates the matching works on.

## Usage

``` r
.universeCovariates(counts)
```

## Arguments

- counts:

  `RegionSetDE.counts` object.

## Value

A data.frame with one row per row of the object.

## Author

Sebastian Gregoricchio
