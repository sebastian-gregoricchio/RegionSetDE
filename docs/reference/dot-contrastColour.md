# .contrastColour

Returns black or white for every colour given, whichever of the two
reads against it.

## Usage

``` r
.contrastColour(colour)
```

## Arguments

- colour:

  Character vector with the colours of the cells.

## Value

A character vector of the same length, holding `"black"` and `"white"`.

## Author

Sebastian Gregoricchio
