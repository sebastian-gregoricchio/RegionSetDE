% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSets.R
\name{plotSetEffect}
\alias{plotSetEffect}
\title{plotSetEffect}
\usage{
plotSetEffect(
  setResults,
  value = "delta.log2FC",
  contrast = NULL,
  colourBy = "FDR",
  FDR = NULL,
  showN = TRUE,
  orderBy = "effect",
  colours = NULL,
  pointSize = 2.5,
  title = NULL,
  subtitle = NULL,
  legendPosition = "right",
  baseSize = 12
)
}
\arguments{
\item{setResults}{\code{RegionSetDE.setResults} or \code{RegionSetDE.setResultsList} object.}

\item{value}{String with the quantity on the axis, either \code{"delta.log2FC"} (the difference with the background) or \code{"mean.log2FC"} (the shift away from zero). Default: \code{"delta.log2FC"}.}

\item{contrast}{String with the name of the contrast to draw, or its position, when \code{setResults} holds several of them. Default: \code{NULL}.}

\item{colourBy}{String with the variable driving the colour, either \code{"FDR"}, \code{"direction"} or \code{"none"}. Default: \code{"FDR"}.}

\item{FDR}{Numeric value with the adjusted p-value cut-off used by the colouring. Default: \code{NULL}, the threshold stored in the object.}

\item{showN}{Logical value to indicate whether the number of regions must be written next to each set. Default: \code{TRUE}.}

\item{orderBy}{String with the ordering of the sets, either \code{"effect"} or \code{"name"}. Default: \code{"effect"}.}

\item{colours}{Character vector of length two with the colours of the significant and non-significant sets. Default: \code{NULL}.}

\item{pointSize}{Numeric value with the size of the points. Default: \code{2.5}.}

\item{title}{String with the title of the plot, rendered as markdown. Default: \code{NULL}, the contrast.}

\item{subtitle}{String with the subtitle of the plot, rendered as markdown. Default: \code{NULL}.}

\item{legendPosition}{String with the position of the legend. Default: \code{"right"}.}

\item{baseSize}{Numeric value with the base font size. Default: \code{12}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the effect size of every region set with its confidence interval, which is the figure the set level conclusion should rest on. The interval carries the inflation for the correlation between the regions, so a set of thirty thousand promoters does not come out looking thirty thousand times more certain than a set of two hundred enhancers.
}
\details{
\code{"mean.log2FC"} is a self-contained quantity and moves with any global shift of the mark, including one left behind by an imperfect normalisation. \code{"delta.log2FC"} is the difference between the set and what it was compared against, and a scaling error common to both cancels out of it. When the two tell different stories, the second is the one that survives a reviewer.

Both levels of the colour scale are kept in the legend even when only one of them occurs, so that a figure in which nothing reaches the cut-off still says what the cut-off was.
}
\examples{
fit <- loadExampleData("fit", verbose = FALSE)
setResults <- testRegionSets(fit, contrast = c("condition", "SHR", "BN"),
                             verbose = FALSE)

# One point per set, with its confidence interval
plotSetEffect(setResults)

plotSetEffect(setResults, value = "mean.log2FC", orderBy = "name")

}
\seealso{
\code{\link{testRegionSets}}, \code{\link{plotSetDistribution}}, \code{\link{plotSetSignal}}
}
\author{
Sebastian Gregoricchio
}
