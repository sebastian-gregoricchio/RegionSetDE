% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotResults.R
\name{plotResultsMA}
\alias{plotResultsMA}
\title{plotResultsMA}
\usage{
plotResultsMA(
  results,
  set = NULL,
  contrast = NULL,
  colourBy = "diff.status",
  facetBySet = TRUE,
  facetScales = "fixed",
  FDR = NULL,
  log2FC = NULL,
  showCounts = TRUE,
  showTrend = TRUE,
  colours = NULL,
  pointSize = 0.8,
  maxPoints = 20000,
  title = NULL,
  subtitle = NULL,
  legendPosition = "right",
  baseSize = 12
)
}
\arguments{
\item{results}{\code{RegionSetDE.results} or \code{RegionSetDE.resultsList} object.}

\item{set}{Character vector with the names of the region sets to draw. Default: \code{NULL}, all of them.}

\item{contrast}{String with the name of the contrast to draw, or its position, when \code{results} holds several of them. Default: \code{NULL}.}

\item{colourBy}{String with the variable driving the colour, either \code{"diff.status"} or \code{"region.set"}. Default: \code{"diff.status"}.}

\item{facetBySet}{Logical value to indicate whether each region set must get its own panel. Default: \code{TRUE}.}

\item{facetScales}{String with the scales of the panels, one among \code{"fixed"}, \code{"free"}, \code{"free_x"} and \code{"free_y"}. Default: \code{"fixed"}.}

\item{FDR}{Numeric value with the adjusted p-value cut-off used to label the points. Default: \code{NULL}, the threshold stored in the object.}

\item{log2FC}{Numeric value with the absolute log2 fold change cut-off used to label the points. Default: \code{NULL}, the threshold stored in the object.}

\item{showCounts}{Logical value to indicate whether the number of changing regions must be written in the corners of each panel, on the right hand side and on the same side of zero as the regions they count. Default: \code{TRUE}.}

\item{showTrend}{Logical value to indicate whether a loess trend must be drawn. Default: \code{TRUE}.}

\item{colours}{Named character vector with the colours. Default: \code{NULL}.}

\item{pointSize}{Numeric value with the size of the points. Default: \code{0.8}.}

\item{maxPoints}{Numeric value with the number of non-changing points drawn per panel. Default: \code{20000}.}

\item{title}{String with the title of the plot, rendered as markdown. Default: \code{NULL}, the contrast.}

\item{subtitle}{String with the subtitle of the plot, rendered as markdown. Default: \code{NULL}.}

\item{legendPosition}{String with the position of the legend. Default: \code{"right"}.}

\item{baseSize}{Numeric value with the base font size. Default: \code{12}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the log2 fold change of a contrast against the average signal, one panel per region set, which shows whether the response depends on how much signal a region carried to begin with.
}
\details{
A trend that leaves zero at one end of the abundance range is the usual sign that the normalisation has not done its job, and it is easier to see here than on any summary statistic. This plot answers a different question from \code{\link{plotSetMA}}, which compares samples before any model is fitted: here the y axis is a fitted coefficient rather than a difference between two libraries.
}
\examples{
\dontrun{
plotResultsMA(res)

plotResultsMA(res, facetScales = "free_x", showTrend = TRUE)
}

}
\seealso{
\code{\link{plotVolcano}}, \code{\link{plotSetMA}}
}
\author{
Sebastian Gregoricchio
}
