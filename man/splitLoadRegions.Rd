% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splitLoadRegions.R
\name{splitLoadRegions}
\alias{splitLoadRegions}
\title{splitLoadRegions}
\usage{
splitLoadRegions(
  regions,
  splitBy = "name",
  header = FALSE,
  selectedSets = NULL,
  minRegionsPerSet = 1,
  maxSets = 100,
  keepSplitColumn = FALSE,
  keepMetadata = TRUE,
  sortRegions = TRUE,
  reduceRegions = FALSE,
  removeDuplicatedRegions = TRUE,
  duplicatedSets = "stop",
  seqlevelsStyle = "UCSC",
  genomeAssembly = NULL,
  outputFormat = "RegionSetDE",
  verbose = TRUE
)
}
\arguments{
\item{regions}{A \code{GRanges} object, a data.frame with chromosome/start/end columns, or a string indicating the path to a BED-like or tabular file containing all the regions.}

\item{splitBy}{String with the name of the column/metadata field containing the region set labels, or numeric value with its position in the input. The chromosome column (\code{"seqnames"}) is accepted as well, to split the regions by chromosome. Default: \code{"name"}, the fourth column of a BED file.}

\item{header}{Logical value to indicate whether the file contains a header line. Used only when \code{regions} is a file path. Notice that headerless files are read as BED and their coordinates are converted from 0-based to 1-based, while data.frames and \code{GRanges} are assumed to be 1-based already. Default: \code{FALSE}.}

\item{selectedSets}{Character vector with the labels of the sets to import, the others being discarded. Default: \code{NULL}, all the sets are imported.}

\item{minRegionsPerSet}{Numeric value indicating the minimum number of regions that a set must contain to be imported. Sets falling below this threshold are dropped with a warning. Default: \code{1}.}

\item{maxSets}{Numeric value indicating the maximum number of sets tolerated. Splitting on a continuous column, such as the score, would generate thousands of single-region sets, therefore the loading is interrupted above this threshold. Default: \code{100}.}

\item{keepSplitColumn}{Logical value to indicate whether the splitting column must be kept in the metadata. Since it is constant within each set, it is redundant with the set name. Default: \code{FALSE}.}

\item{keepMetadata}{Logical value to indicate whether the metadata columns must be kept. Default: \code{TRUE}.}

\item{sortRegions}{Logical value to indicate whether the regions must be sorted by coordinate. Default: \code{TRUE}.}

\item{reduceRegions}{Logical value to indicate whether the overlapping regions within the same set must be collapsed (\code{IRanges::reduce}). Notice that metadata are lost upon reduction. Default: \code{FALSE}.}

\item{removeDuplicatedRegions}{Logical value to indicate whether the regions with identical coordinates within the same set must be collapsed to a single entry. Default: \code{TRUE}.}

\item{duplicatedSets}{String indicating how to handle two or more sets containing exactly the same regions: \code{"stop"}, \code{"remove"} or \code{"keep"}. Default: \code{"stop"}.}

\item{seqlevelsStyle}{String indicating the chromosome naming style to apply, one among \code{"UCSC"} (chr1), \code{"Ensembl"} (1) or \code{"NCBI"}. Default: \code{"UCSC"}.}

\item{genomeAssembly}{String indicating the genome assembly to store in the ranges seqinfo, e.g. \code{"hg38"}. Default: \code{NULL}, no assembly is assigned.}

\item{outputFormat}{String indicating the class of the returned object, one among \code{"RegionSetDE"}, \code{"GRangesList"} or \code{"list"}. Default: \code{"RegionSetDE"}.}

\item{verbose}{Logical value to indicate whether the loading messages must be printed. Default: \code{TRUE}.}
}
\value{
A \code{RegionSetDE} object or, depending on \code{outputFormat}, a named \code{GRangesList} or a named list of \code{GRanges}.
}
\description{
Imports a collection of genomic regions stored in a single object or file and splits them into individual region sets according to the values of one of its columns. The regions can be provided as a \code{GRanges}, a data.frame or the path to a BED-like/tabular file. All the arguments controlling the import are passed to \code{\link{loadRegions}}.
}
\examples{
regionTable <- loadExampleData("regions", verbose = FALSE)

regionRanges <- GenomicRanges::makeGRangesFromDataFrame(regionTable,
                                                        keep.extra.columns = TRUE)

regions <- splitLoadRegions(regionRanges,
                            splitBy = "setName",
                            genomeAssembly = "rn4",
                            verbose = FALSE)
regionSetNames(regions)

\dontrun{
regions <- splitLoadRegions("peaks/all_peaks_annotated.bed",
                            splitBy = "name",
                            genomeAssembly = "hg38")
}

}
\seealso{
\code{\link{loadRegions}}
}
\author{
Sebastian Gregoricchio
}
