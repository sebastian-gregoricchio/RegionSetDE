% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fitRegions.R
\name{.fitLimma}
\alias{.fitLimma}
\title{.fitLimma}
\usage{
.fitLimma(
  countMatrix,
  designMatrix,
  librarySizes,
  offsetMatrix,
  colTable,
  block = NULL,
  robust = TRUE,
  priorCount = 2,
  verbose = TRUE
)
}
\arguments{
\item{countMatrix}{Numeric matrix with the values.}

\item{designMatrix}{Design matrix.}

\item{librarySizes}{Numeric vector with the library size of each sample.}

\item{offsetMatrix}{Numeric matrix of log offsets, or \code{NULL}.}

\item{colTable}{Data.frame with the sample annotation.}

\item{block}{String naming a \code{colData} column holding a blocking variable, or \code{NULL}. Default: \code{NULL}.}

\item{robust}{Logical value to indicate whether the prior variance must be estimated robustly. Default: \code{TRUE}.}

\item{priorCount}{Numeric value added before the logarithm. Default: \code{2}.}

\item{verbose}{Logical value to indicate whether the messages must be printed. Default: \code{TRUE}.}
}
\value{
A list with the fit, the blocking information and the median residual standard deviation.
}
\description{
Fits the \code{limma} model on the log2 signal, with the residual variance trended on abundance, for values that are not counts.
}
\details{
Where \code{voom} estimates a weight per observation from a mean-variance trend fitted on the count scale, this fits the trend on the residual variance itself and applies no weights. Nothing about the values then has to be a fragment count, which is what makes it the engine for coverage, for an externally normalised matrix, or for anything else that arrives already on a continuous scale.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
