% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSets.R
\name{plotSetDistribution}
\alias{plotSetDistribution}
\title{plotSetDistribution}
\usage{
plotSetDistribution(
  setResults,
  style = "violin",
  set = NULL,
  contrast = NULL,
  annotate = TRUE,
  colours = NULL,
  title = NULL,
  subtitle = NULL,
  legendPosition = NULL,
  baseSize = 12
)
}
\arguments{
\item{setResults}{\code{RegionSetDE.setResults} or \code{RegionSetDE.setResultsList} object.}

\item{style}{String with the kind of plot, one of \code{"violin"}, \code{"boxplot"} and \code{"ecdf"}. Default: \code{"violin"}.}

\item{set}{Character vector with the names of the region sets to draw. Default: \code{NULL}, all of them.}

\item{contrast}{String with the name of the contrast to draw, or its position, when \code{setResults} holds several of them. Default: \code{NULL}.}

\item{annotate}{Logical value to indicate whether the effect size and the adjusted p-value must be written above each set. Ignored for \code{style = "ecdf"}. Default: \code{TRUE}.}

\item{colours}{Named character vector with one colour per region set. Default: \code{NULL}.}

\item{title}{String with the title of the plot, rendered as markdown. Default: \code{NULL}, the contrast.}

\item{subtitle}{String with the subtitle of the plot, rendered as markdown. Default: \code{NULL}.}

\item{legendPosition}{String with the position of the legend. Default: \code{NULL}, no legend for the violins and the boxplots, where the sets are already named on the axis, and a legend for the cumulative curves, where they are not.}

\item{baseSize}{Numeric value with the base font size. Default: \code{12}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the whole distribution of the per-region log2 fold changes of every set, rather than its summary. A set whose mean has moved because a small group of regions collapsed looks nothing like one whose regions all shifted a little, and only the distribution tells the two apart.
}
\examples{
\dontrun{
plotSetDistribution(setRes)

plotSetDistribution(setRes, style = "ecdf")
}

}
\seealso{
\code{\link{testRegionSets}}, \code{\link{plotSetEffect}}, \code{\link{plotSetSignal}}
}
\author{
Sebastian Gregoricchio
}
