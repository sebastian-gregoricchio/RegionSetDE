% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{RegionSetDE.setScores-class}
\alias{RegionSetDE.setScores-class}
\title{RegionSetDE.setScores class}
\description{
S4 class storing the signal scores computed by \code{\link{scoreRegionSets}}, one per region set per library, together with the paired comparisons between the sets. No contrast lies behind these numbers and no model was fitted on them, which is what separates this class from \code{RegionSetDE.setResults}: the replication is the libraries, and the comparison runs between sets rather than between conditions.
}
\section{Slots}{

\describe{
\item{\code{scores}}{Data.frame with one row per library per set, carrying the summarised signal, the reference it was divided by and the \code{score} on a log2 scale.}

\item{\code{comparisons}}{Data.frame with one row per pair of sets, carrying the mean paired difference between their scores, its confidence interval, the paired t-statistic and the adjusted p-value.}

\item{\code{sample.metadata}}{Data.frame with the \code{colData} of the counts object the scores came from, so that the libraries can be grouped and coloured downstream without handing that object over again.}

\item{\code{reference}}{String naming what the signal of a set was divided by, one among \code{"background"}, \code{"regions"} and \code{"none"}.}

\item{\code{assay}}{String with the name of the assay the signal was read from.}

\item{\code{summary}}{String with how the regions of a set were summarised, either \code{"mean"} or \code{"median"}.}

\item{\code{per.basepair}}{Logical value indicating whether the signal was divided by the width of the region before being summarised.}
}}

\seealso{
\code{\link{scoreRegionSets}}
}
\author{
Sebastian Gregoricchio
}
