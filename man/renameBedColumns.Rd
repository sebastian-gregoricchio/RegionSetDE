% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regionsUtils.R
\name{renameBedColumns}
\alias{renameBedColumns}
\title{renameBedColumns}
\usage{
renameBedColumns(table, bedFormat = 3)
}
\arguments{
\item{table}{A data.frame whose first columns hold the genomic coordinates.}

\item{bedFormat}{Numeric value indicating how many columns must be renamed, one among \code{3} (seqnames, start, end), \code{6} (adding name, score, strand), \code{9} (adding thickStart, thickEnd, itemRgb) or \code{12} (adding blockCount, blockSizes, blockStarts). Default: \code{3}.}
}
\value{
The input data.frame with the first columns renamed.
}
\description{
Assigns the standard BED column names to the first columns of a data.frame, leaving the remaining ones untouched. Useful to convert a headerless table, read with generic \code{V1}, \code{V2}, \code{V3} names, into a data.frame ready for \code{GenomicRanges::makeGRangesFromDataFrame}.
}
\examples{
bedTable <- data.frame(V1 = "chr12",
                       V2 = c(1000, 5000),
                       V3 = c(2000, 6000),
                       V4 = c("peak_1", "peak_2"),
                       V5 = c(120, 340),
                       V6 = c("+", "-"))

renameBedColumns(bedTable, bedFormat = 6)

# Only the first three columns are renamed, the rest keep their names
renameBedColumns(bedTable, bedFormat = 3)

}
\author{
Sebastian Gregoricchio
}
