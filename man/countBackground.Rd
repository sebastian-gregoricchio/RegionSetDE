% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countBackground.R
\name{countBackground}
\alias{countBackground}
\title{countBackground}
\usage{
countBackground(
  counts,
  bamFiles = NULL,
  binSize = 10000,
  excludeRegions = TRUE,
  minCount = 1,
  restrictChromosomes = NULL,
  pairedEnd = NULL,
  fragmentLength = NULL,
  maxFragmentLength = NULL,
  minMapq = NULL,
  removeDuplicates = NULL,
  nThreads = 1,
  verbose = TRUE
)
}
\arguments{
\item{counts}{\code{RegionSetDE.counts} object returned by \code{\link{countReads}}.}

\item{bamFiles}{Character vector with the paths of the BAM files, in the same order as the samples of \code{counts}. Default: \code{NULL}, the files recorded by \code{\link{countReads}} are reused.}

\item{binSize}{Numeric value with the width of the bins, in base pairs. Default: \code{10000}.}

\item{excludeRegions}{Logical value indicating whether the bins overlapping the regions of \code{counts} must be discarded. Default: \code{TRUE}.}

\item{minCount}{Numeric value with the minimum total count required to keep a bin. Default: \code{1}.}

\item{restrictChromosomes}{Character vector with the chromosomes to read from the BAM files. Default: \code{NULL}, the value used at the counting step.}

\item{pairedEnd}{Logical value, or one logical value per BAM file, indicating whether the reads must be counted as proper pairs. Default: \code{NULL}, the layouts resolved at the counting step.}

\item{fragmentLength}{Numeric value with the length to which single-end reads are extended. Default: \code{NULL}, the value used at the counting step.}

\item{maxFragmentLength}{Numeric value with the maximum insert size accepted for a pair. Default: \code{NULL}, the value used at the counting step.}

\item{minMapq}{Numeric value with the minimum mapping quality of a read. Default: \code{NULL}, the value used at the counting step.}

\item{removeDuplicates}{Logical value indicating whether the duplicated reads must be discarded. Default: \code{NULL}, the value used at the counting step.}

\item{nThreads}{Number of threads used to process the files in parallel. Default: \code{1}.}

\item{verbose}{Logical value to indicate whether the messages must be printed. Default: \code{TRUE}.}
}
\value{
The input \code{RegionSetDE.counts} object with the bin counts stored as a \code{RangedSummarizedExperiment} in \code{metadata(counts)$background}.
}
\description{
Counts the reads falling in large genome wide bins, which provide the background distribution used to estimate composition-aware normalisation factors. The bins overlapping the counted regions are removed by default, so that the normalisation is not driven by the signal under study. The result is stored in the metadata of the counts object, where the normalisation step retrieves it.
}
\details{
Bins of ten kilobases or more are wide enough that most of them carry background reads only, and their counts therefore track the amount of sequencing spent outside the regions of interest. Reusing the read parameters of \code{\link{countReads}} matters here: bins counted with a different mapping quality or duplicate policy would return factors that do not apply to the region counts. The parameters are taken from the object unless they are given explicitly.
}
\examples{
# The example counts already carry their background bins
counts <- loadExampleData("counts", verbose = FALSE)

backgroundBins <- S4Vectors::metadata(counts)$background
backgroundBins

\dontrun{
# Recomputing them needs the BAM files the object was counted from
counts <- countBackground(counts, binSize = 10000, nThreads = 4)
}

}
\seealso{
\code{\link{countReads}}
}
\author{
Sebastian Gregoricchio
}
