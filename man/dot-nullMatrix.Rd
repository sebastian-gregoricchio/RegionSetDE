% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimateNullDispersion.R
\name{.nullMatrix}
\alias{.nullMatrix}
\title{.nullMatrix}
\usage{
.nullMatrix(
  counts,
  source = "background",
  regionSets = NULL,
  index = NULL,
  subset = NULL,
  minCount = 10,
  maxRows = 50000
)
}
\arguments{
\item{counts}{\code{RegionSetDE.counts} object.}

\item{source}{String with where the null rows come from.}

\item{regionSets}{Character vector with the sets used as null rows, or \code{NULL}.}

\item{index}{Integer vector with the positions of the null rows, or \code{NULL}.}

\item{subset}{Integer vector restricting the selected rows further, or \code{NULL}.}

\item{minCount}{Numeric value with the average count a row must carry.}

\item{maxRows}{Numeric value with the number of rows kept.}
}
\value{
A list with the filtered \code{counts} and \code{offset} matrices, the \code{library.size} vector, the \code{abundance} of the kept rows and \code{kept.rows}, their positions in the unfiltered selection.
}
\description{
Collects the counts and the offsets of the rows a null estimate is computed on.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
