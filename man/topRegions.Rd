% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resultsMethods.R
\name{topRegions}
\alias{topRegions}
\title{topRegions}
\usage{
topRegions(
  results,
  n = Inf,
  set = NULL,
  contrast = NULL,
  sortBy = "FDR",
  direction = "both",
  FDR = NULL,
  log2FC = NULL,
  level = "region"
)
}
\arguments{
\item{results}{\code{RegionSetDE.results} or \code{RegionSetDE.resultsList} object.}

\item{n}{Numeric value with the number of regions to return. When \code{set} holds more than one name, \code{n} regions are returned for each of them. Default: \code{Inf}, every region passing the thresholds.}

\item{set}{Character vector with the names of the region sets to consider. Default: \code{NULL}, all of them pooled together.}

\item{contrast}{String with the name of the contrast to take, or its position, when \code{results} holds several of them. Default: \code{NULL}.}

\item{sortBy}{String with the column driving the ranking, one of \code{"FDR"}, \code{"p.value"}, \code{"log2FC"} and \code{"stat"}. Default: \code{"FDR"}.}

\item{direction}{String restricting the output to one direction of change, one of \code{"both"}, \code{"up"} and \code{"down"}. Default: \code{"both"}.}

\item{FDR}{Numeric value with an adjusted p-value cut-off applied before the ranking. Default: \code{NULL}, the threshold stored in the object.}

\item{log2FC}{Numeric value with an absolute log2 fold change cut-off applied before the ranking. Default: \code{NULL}, the threshold stored in the object.}

\item{level}{String indicating whether the regions (\code{"region"}) or the tiles (\code{"tile"}) must be ranked. Default: \code{"region"}.}
}
\value{
A data.frame with the selected rows.
}
\description{
Returns the regions that respond most strongly to a contrast, optionally restricted to one region set or to one direction.
}
\details{
Ranking by \code{"log2FC"} sorts on the effect size alone and returns whatever passes the cut-offs, which on a small object is often a handful of low-count regions with a large and badly estimated fold change. Keep an FDR cut-off in place when doing so.
}
\examples{
\dontrun{
topRegions(res)

topRegions(res, n = 20)

# Twenty per set, so that a large set does not fill the table
topRegions(res, n = 20, set = c("tss", "enhancers"))

topRegions(res, n = 50, direction = "down", sortBy = "log2FC", FDR = 0.01)

topRegions(resList, contrast = "combo", set = "enhancers")
}

}
\seealso{
\code{\link{testRegions}}, \code{\link{plotVolcano}}
}
\author{
Sebastian Gregoricchio
}
