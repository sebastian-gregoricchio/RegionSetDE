% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/AllClasses.R
\docType{class}
\name{RegionSetDE.setResults-class}
\alias{RegionSetDE.setResults-class}
\title{RegionSetDE.setResults class}
\description{
S4 class holding the outcome of a set level test. The table in the \code{results} slot has one row per region set and carries the effect size with its confidence interval next to the p-values, since with tens of thousands of regions in a set the p-value stops discriminating long before the effect size does.
}
\section{Slots}{

\describe{
\item{\code{results}}{Data.frame with one row per region set, or per pair of sets for a set contrast.}

\item{\code{regionStats}}{Data.frame with the per-region statistics the set level test was computed on.}

\item{\code{contrast}}{String describing the contrast that was tested.}

\item{\code{contrast.groups}}{List with the \code{column} of the \code{colData} the contrast separates and the two \code{groups} it compares, the first one being the level the fold change is positive for. Empty when the contrast is not a difference between two levels of one variable.}

\item{\code{test}}{String with the kind of test, either \code{"set"} or \code{"setContrast"}.}

\item{\code{methods}}{Character vector with the tests that were run.}

\item{\code{universe}}{\code{RegionSetDE.universe} object with the rows every set was compared against. Empty when the test was self-contained only.}

\item{\code{engine}}{String with the engine that produced the per-region statistics.}

\item{\code{thresholds}}{List with the FDR cut-off and the correction method.}

\item{\code{counts}}{\code{RegionSetDE.counts} object the test was computed on, so that the signal behind a set level claim can be drawn without carrying a second object around. Empty when \code{carryCounts} was \code{FALSE}.}
}}

\author{
Sebastian Gregoricchio
}
