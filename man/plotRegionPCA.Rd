% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotSampleQC.R
\name{plotRegionPCA}
\alias{plotRegionPCA}
\title{plotRegionPCA}
\usage{
plotRegionPCA(
  object,
  set = NULL,
  contrast = NULL,
  colourBy = NULL,
  shapeBy = NULL,
  labelBy = "sample",
  useOffsets = TRUE,
  compareOffsets = FALSE,
  facetBySet = FALSE,
  dimensions = c(1, 2),
  topRegions = 2000,
  pointSize = 3,
  colours = NULL,
  title = NULL,
  subtitle = NULL,
  legendPosition = "right",
  baseSize = 12
)
}
\arguments{
\item{object}{\code{RegionSetDE.counts}, \code{RegionSetDE.fit} or any result object of the package.}

\item{set}{Character vector with the names of the region sets used. Default: \code{NULL}, all of them.}

\item{contrast}{String with the name of a contrast, or its position, when \code{object} holds several of them. Default: \code{NULL}.}

\item{colourBy}{String with the name of a \code{colData} column driving the colour. Default: \code{NULL}.}

\item{shapeBy}{String with the name of a \code{colData} column driving the shape. Default: \code{NULL}.}

\item{labelBy}{String with the name of a \code{colData} column written next to the points, or \code{"sample"}. Default: \code{"sample"}.}

\item{useOffsets}{Logical value to indicate whether the normalisation stored in the object must be applied. Default: \code{TRUE}.}

\item{compareOffsets}{Logical value to indicate whether the same ordination must be drawn twice, once with the normalisation and once on the library sizes alone. Default: \code{FALSE}.}

\item{facetBySet}{Logical value to indicate whether each region set must get its own ordination. Default: \code{FALSE}.}

\item{dimensions}{Numeric vector of length two with the components drawn. Default: \code{c(1, 2)}.}

\item{topRegions}{Numeric value with the number of most variable regions the ordination is computed on. Default: \code{2000}.}

\item{pointSize}{Numeric value with the size of the points. Default: \code{3}.}

\item{colours}{Named character vector with the colours. Default: \code{NULL}.}

\item{title}{String with the title of the plot, rendered as markdown. Default: \code{NULL}.}

\item{subtitle}{String with the subtitle of the plot, rendered as markdown. Default: \code{NULL}.}

\item{legendPosition}{String with the position of the legend. Default: \code{"right"}.}

\item{baseSize}{Numeric value with the base font size. Default: \code{12}.}
}
\value{
A \code{ggplot} object, carrying the coordinates and the variance explained as the \code{pca} attribute, in the \code{x.variance} and \code{y.variance} columns. The variance explained is written on the axis titles when a single panel is drawn, and in the panel labels when there are several, since every panel recomputes its own components.
}
\description{
Places the samples on the first principal components of the region signal, which is the fastest way to see whether the conditions separate, whether the replicates pair, and whether either of those is really the sequencing depth in disguise.
}
\details{
\code{compareOffsets} is the argument worth using. Scaling factors estimated outside the object, from a spike-in or a greenlist, impose a grouping of their own, and when that grouping happens to match the replicates it is indistinguishable from a batch effect until the two ordinations are put side by side. A separation that survives the normalisation being removed is in the data; one that appears only with it is the factors writing themselves into the ordination, and blocking on it would be blocking on an artefact.

The regions are the same in both panels, chosen once by variance on the normalised values, so what differs between them is the transformation and not the selection. Restricting to the most variable rows is what makes an ordination read the structure rather than the depth, and \code{topRegions} controls how aggressively.

Marks and assays should not share an ordination any more than they share a model. Split with \code{\link{splitSamples}} first, or use \code{facetBySet} when the sets themselves are the question.
}
\examples{
counts <- loadExampleData("counts", verbose = FALSE)
counts <- normalizeCounts(counts, method = "background", verbose = FALSE)

plotRegionPCA(counts, colourBy = "condition", shapeBy = "sex")

# Restricted to one set, which is where the strain effect should show
plotRegionPCA(counts, set = "promoterCpG", colourBy = "condition")

}
\seealso{
\code{\link{plotSampleCorrelation}}, \code{\link{normalizeCounts}}, \code{\link{splitSamples}}
}
\author{
Sebastian Gregoricchio
}
