% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countsUtils.R
\name{.flattenRegionSets}
\alias{.flattenRegionSets}
\title{.flattenRegionSets}
\usage{
.flattenRegionSets(
  regionSet,
  tileWidth = NULL,
  partialTiles = TRUE,
  keepMetadata = TRUE,
  regionId = NULL,
  verbose = TRUE
)
}
\arguments{
\item{regionSet}{\code{RegionSetDE} object, \code{GRangesList} or named list of \code{GRanges}.}

\item{tileWidth}{Numeric value with the width of the tiles. Default: \code{NULL}, one row per region.}

\item{partialTiles}{Logical value indicating whether the trailing shorter tile must be kept. Default: \code{TRUE}.}

\item{keepMetadata}{Logical value to indicate whether the metadata columns of the regions must be carried over. Default: \code{TRUE}.}

\item{regionId}{String with the name of a metadata column holding the region identifiers. Default: \code{NULL}, the names of the ranges, and their coordinates when they are unnamed.}

\item{verbose}{Logical value to indicate whether the messages must be printed. Default: \code{TRUE}.}
}
\value{
A named \code{GRanges} with the \code{region.set}, \code{region.id} and \code{tile.id} metadata columns, followed by whatever the regions carried.
}
\description{
Turns a collection of region sets into a single \code{GRanges}, one element per row of the future counts matrix. The set name and the region identifier are stored in the metadata columns, whatever else the regions carried is harmonised across the sets and kept alongside them, and the regions are optionally cut into tiles.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
