% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/loadSampleSheet.R
\name{.sheetCountingInput}
\alias{.sheetCountingInput}
\title{.sheetCountingInput}
\usage{
.sheetCountingInput(
  regionSet,
  sampleSheet,
  files,
  sampleNames,
  sampleMetadata,
  fileField,
  verbose = TRUE
)
}
\arguments{
\item{regionSet}{Object handed to the counting function.}

\item{sampleSheet}{Data.frame returned by \code{loadSampleSheet}, the path to a sample sheet, or \code{NULL}.}

\item{files}{Character vector with the files given directly, or \code{NULL}.}

\item{sampleNames}{Character vector with the sample names given directly, or \code{NULL}.}

\item{sampleMetadata}{Data.frame with the annotation given directly, or \code{NULL}.}

\item{fileField}{String with the column of the sheet holding the files, either \code{"bam"} or \code{"bigwig"}.}

\item{verbose}{Logical value to indicate whether the messages must be printed. Default: \code{TRUE}.}
}
\value{
A list with the \code{files}, the \code{sampleNames} and the \code{sampleMetadata} to count with.
}
\description{
Takes the signal files, the sample names and the annotation of the counting from a sample sheet, or from the sample sheet a consensus was built from when no file is given at all.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
