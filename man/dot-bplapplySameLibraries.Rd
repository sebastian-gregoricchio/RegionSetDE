% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countsUtils.R
\name{.bplapplySameLibraries}
\alias{.bplapplySameLibraries}
\title{.bplapplySameLibraries}
\usage{
.bplapplySameLibraries(X, FUN, ..., BPPARAM)
}
\arguments{
\item{X}{List or vector to iterate over.}

\item{FUN}{Function applied to each element.}

\item{...}{Further arguments of \code{FUN}.}

\item{BPPARAM}{\code{BiocParallelParam} object.}
}
\value{
The list returned by \code{BiocParallel::bplapply}.
}
\description{
Runs \code{BiocParallel::bplapply} with socket workers that load the packages from the same libraries as the calling session. A socket worker is a new R process: the functions it receives refer to the RegionSetDE namespace by name, and the worker loads that namespace from the libraries it starts with, which are not the ones the session may have added with \code{.libPaths()}. When the two disagree, as during \code{R CMD build}, where the package being built sits in a temporary library, the worker runs new code against an older installed namespace, or finds no package at all. Forked and serial workers share the session and need nothing.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
