% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/applyFilters.R
\name{applyBlacklist}
\alias{applyBlacklist}
\title{applyBlacklist}
\usage{
applyBlacklist(
  regionSet,
  blacklist,
  overlapType = "any",
  minOverlapBp = 1,
  minOverlapFraction = 0,
  trimRegions = FALSE,
  ignoreStrand = TRUE,
  emptySets = "stop",
  verbose = TRUE
)
}
\arguments{
\item{regionSet}{A \code{RegionSetDE} object, a \code{GRangesList}, a named list of \code{GRanges} or a single \code{GRanges}.}

\item{blacklist}{String indicating the path to a BED-like file, a \code{GRanges} or a data.frame with the regions to exclude.}

\item{overlapType}{String indicating the type of overlap required to blacklist a region, one among \code{"any"}, \code{"within"}, \code{"start"}, \code{"end"} or \code{"equal"}. Default: \code{"any"}.}

\item{minOverlapBp}{Numeric value indicating the minimum number of bases that must overlap the blacklist for a region to be removed. Default: \code{1}.}

\item{minOverlapFraction}{Numeric value between 0 and 1 indicating the minimum fraction of a region that must overlap the blacklist for it to be removed. Default: \code{0}, any overlap is sufficient.}

\item{trimRegions}{Logical value to indicate whether the blacklisted portion must be subtracted from the regions instead of removing them entirely. Notice that trimming collapses the regions overlapping each other within the same set. Default: \code{FALSE}.}

\item{ignoreStrand}{Logical value to indicate whether the strand must be ignored when computing the overlaps. Default: \code{TRUE}.}

\item{emptySets}{String indicating how to handle the sets left without any region, one among \code{"stop"}, \code{"remove"} or \code{"keep"}. Default: \code{"stop"}.}

\item{verbose}{Logical value to indicate whether the filtering messages must be printed. Default: \code{TRUE}.}
}
\value{
An object of the same class as the input, with the blacklisted regions removed. For a \code{RegionSetDE} object the blacklist and the filtering counts are stored in the corresponding slots.
}
\description{
Removes from every region set the regions overlapping a blacklist, such as the ENCODE blacklisted regions. The blacklist can be provided as a BED-like file, a \code{GRanges} or a data.frame.
}
\examples{
\dontrun{
regions <- applyBlacklist(regions, blacklist = "hg38-blacklist.v2.bed")
}

}
\seealso{
\code{\link{applyWhitelist}}, \code{\link{loadRegions}}
}
\author{
Sebastian Gregoricchio
}
