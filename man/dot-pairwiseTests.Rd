% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotResults.R
\name{.pairwiseTests}
\alias{.pairwiseTests}
\title{.pairwiseTests}
\usage{
.pairwiseTests(
  plotTable,
  colTable,
  test = "t.test",
  pairBy = NULL,
  comparisons = NULL,
  groupLevels,
  pAdjustMethod = "none"
)
}
\arguments{
\item{plotTable}{Data.frame with one row per sample and the \code{sample}, \code{value} and \code{group} columns.}

\item{colTable}{Data.frame with the sample metadata and a \code{sample} column.}

\item{test}{String with the test, either \code{"t.test"} or \code{"wilcox.test"}. Default: \code{"t.test"}.}

\item{pairBy}{String with the column pairing the samples, or \code{NULL} for unpaired tests. Default: \code{NULL}.}

\item{comparisons}{List of character vectors of length two, or \code{NULL} for every pair of groups. Default: \code{NULL}.}

\item{groupLevels}{Character vector with the groups, in the order of the axis.}

\item{pAdjustMethod}{String with the correction applied across the comparisons. Default: \code{"none"}.}
}
\value{
A data.frame with one row per comparison and the \code{group1}, \code{group2}, \code{n.first}, \code{n.second}, \code{p.value} and \code{p.adjusted} columns. With \code{pairBy} the two sizes are the number of pairs.
}
\description{
Runs a two-sample test between each pair of groups on the values of one region, as they are drawn, pairing the samples through a column of the metadata when asked to.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
