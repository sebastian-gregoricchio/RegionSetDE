% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkNullCalibration.R
\name{plotNullCalibration}
\alias{plotNullCalibration}
\title{plotNullCalibration}
\usage{
plotNullCalibration(
  calibration,
  style = "histogram",
  bins = 50,
  colours = NULL,
  title = NULL,
  subtitle = NULL,
  legendPosition = "none",
  baseSize = 12
)
}
\arguments{
\item{calibration}{List returned by \code{\link{checkNullCalibration}}.}

\item{style}{String with the kind of plot, one of \code{"histogram"}, \code{"qq"} and \code{"abundance"}. Default: \code{"histogram"}.}

\item{bins}{Numeric value with the number of bins of the histogram. Default: \code{50}.}

\item{colours}{Character vector of length two with the colours of the data and of the expected line. Default: \code{NULL}.}

\item{title}{String with the title of the plot, rendered as markdown. Default: \code{NULL}, the contrast.}

\item{subtitle}{String with the subtitle of the plot, rendered as markdown. Default: \code{NULL}, the dispersion and the verdict.}

\item{legendPosition}{String with the position of the legend. Default: \code{"none"}.}

\item{baseSize}{Numeric value with the base font size. Default: \code{12}.}
}
\value{
A \code{ggplot} object.
}
\description{
Draws the p-values of the rows that should not respond to a contrast, against the flat distribution they would follow if the dispersion were right.
}
\details{
A flat histogram sitting on the expected line is the target. Bars piling up near zero mean the dispersion is too low and the fit is calling library-to-library variation a treatment effect; the same optimism is in the regions, at the same rate. A histogram sagging near zero means the opposite, which costs sensitivity and nothing else.

The quantile plot says the same thing with more resolution in the tail, which is where the regions that end up in a figure come from. The abundance plot says where the trouble is: a flat line across the strata means the dispersion is wrong everywhere, while a line rising towards the weak rows means the filter is too loose and \code{\link{filterRegions}} is the fix.
}
\examples{
fit <- loadExampleData("fit", verbose = FALSE)

calibration <- checkNullCalibration(fit,
                                    contrast = c("condition", "SHR", "BN"),
                                    source = "background",
                                    verbose = FALSE)

# A flat histogram is what a well calibrated test looks like
plotNullCalibration(calibration)

plotNullCalibration(calibration, style = "qq")

}
\seealso{
\code{\link{checkNullCalibration}}, \code{\link{estimateNullDispersion}}
}
\author{
Sebastian Gregoricchio
}
