% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bamUtils.R
\name{.readBamFragments}
\alias{.readBamFragments}
\title{.readBamFragments}
\usage{
.readBamFragments(
  job,
  bamFiles,
  pairedEnd,
  fragmentLength,
  maxFragmentLength,
  minMapq,
  removeDuplicates,
  countMode
)
}
\arguments{
\item{job}{List describing the job: \code{file.index}, the \code{pieces} table, the \code{ranges} to count with their \code{range.chromosome} and \code{range.index}, and the \code{discard} regions of its chromosomes.}

\item{bamFiles}{Character vector with the paths of the BAM files.}

\item{pairedEnd}{Logical vector with one value per BAM file.}

\item{fragmentLength}{Numeric value with the length to which single-end reads are extended.}

\item{maxFragmentLength}{Numeric value with the maximum length of a paired-end fragment.}

\item{minMapq}{Numeric value with the minimum mapping quality of a read.}

\item{removeDuplicates}{Logical value indicating whether the reads flagged as duplicates must be discarded.}

\item{countMode}{String, either \code{"overlap"} or \code{"bin"}, see \code{.countBamFragments}.}
}
\value{
A list with the \code{file.index} and \code{range.index} of the job, the \code{counts} of its ranges, the \code{total.fragments} that went through the filters and \code{mate.mapq.found}.
}
\description{
Reads the fragments of one BAM file over a group of pieces of genome and counts them over the ranges of the same chromosomes. It is the job run by every thread of \code{.countBamFragments}.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
