% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/makeSetUniverse.R
\name{.resolveUniverse}
\alias{.resolveUniverse}
\title{.resolveUniverse}
\usage{
.resolveUniverse(
  object,
  universe = "matched",
  matchOn = c("width", "abundance"),
  universeRatio = 5,
  regionSets = NULL,
  soft = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{object}{\code{RegionSetDE.fit} or \code{RegionSetDE.counts} object.}

\item{universe}{String with a keyword, a \code{RegionSetDE.universe} object, or \code{NULL}.}

\item{matchOn}{Character vector with the covariates the comparison rows are matched on.}

\item{universeRatio}{Numeric value with the number of comparison rows drawn per region of the set.}

\item{regionSets}{Character vector with the sets being tested, or \code{NULL}.}

\item{soft}{Logical value to indicate whether a universe that cannot be built must return empty rather than stop. Default: \code{FALSE}.}

\item{verbose}{Logical value to indicate whether the messages must be printed. Default: \code{TRUE}.}
}
\value{
A \code{RegionSetDE.universe} object, empty when none could be built under \code{soft}.
}
\description{
Turns the \code{universe} argument of \code{\link{fitRegions}} and \code{\link{testRegionSets}} into a \code{RegionSetDE.universe} object, building it when a keyword was given rather than an object.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
