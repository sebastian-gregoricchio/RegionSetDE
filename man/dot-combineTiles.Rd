% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testRegions.R
\name{.combineTiles}
\alias{.combineTiles}
\title{.combineTiles}
\usage{
.combineTiles(
  tileTable,
  tileRanges,
  extraColumns = character(0),
  method = "simes",
  directionFDR = 0.05,
  adjustMethod = "BH",
  verbose = TRUE
)
}
\arguments{
\item{tileTable}{Data.frame with one row per tile, as returned by the engine specific test.}

\item{tileRanges}{\code{GRanges} with the coordinates of the tiles.}

\item{extraColumns}{Character vector with the annotation columns carried over from the tiles.}

\item{method}{String with the combination method.}

\item{directionFDR}{Numeric value with the false discovery rate, within each region, below which a tile counts as moving up or down in \code{n.tiles.up} and \code{n.tiles.down}. It is the \code{fc.threshold} of csaw, which despite its name is not a fold change. Default: \code{0.05}.}

\item{adjustMethod}{String with the multiple testing correction.}

\item{verbose}{Logical value to indicate whether the messages must be printed.}
}
\value{
A list with the \code{results} data.frame and the \code{regions} \code{GRanges}.
}
\description{
Combines the tile level statistics into one row per region, through \code{csaw::combineTests}.
}
\author{
Sebastian Gregoricchio
}
\keyword{internal}
